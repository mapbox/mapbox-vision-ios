/// Specifies the concrete type of `ARNode`.
enum ARNodeType {
    /// The type represents `ARRootNode`.
    case rootNode
    /// The type represents `ARGridNode`.
    case gridNode
    /// The type represents `ARLaneNode`.
    case arrowNode
    /// The type represents `ARCameraNode`.
    case cameraNode
}
